services:
  - type: web
    name: rudexmusic
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python -m DAXXMUSIC
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.4
      - key: PORT
        value: 8000
